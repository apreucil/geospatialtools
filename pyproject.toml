# filepath: /ncrc/home2/Anthony.Preucil/apscratch/HydroBlocks/geospatialtools/pyproject.toml
[build-system]
requires = ["setuptools>=61.0", "wheel", "numpy", "scipy>=1.10.0"]
build-backend = "setuptools.build_meta"

[project]
name = "geospatialtools"
version = "0.1.0"
description = "Geospatial tools for terrain and upscaling operations"
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
license = { text = "MIT" }
dependencies = ["numpy", "scipy"]

[tool.setuptools]
packages = ["geospatialtools"]